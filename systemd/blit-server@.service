[Unit]
Description=blit terminal multiplexer for %i
Requires=blit-server@%i.socket

[Service]
# blit server sends sd_notify(READY=1) once the IPC listener is bound.
Type=notify
User=%i
WorkingDirectory=~
ExecStart=blit server
Restart=no
