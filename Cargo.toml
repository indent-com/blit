[workspace]
resolver = "2"
members = [
    "remote",
    "alacritty-driver",
    "server",
    "gateway",
    "browser",
    "demo",
    "cli",
    "fonts",
    "webserver",
]

[profile.release]
opt-level = "s"
lto = true
